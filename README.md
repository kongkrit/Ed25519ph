# Ed25519ph streaming version

totally offline utility. no network activity. no `localStorage`. no cookies. no funny business.

Use [libsodium.js](where) to stream a very large file and generate Ed25519ph signature.
